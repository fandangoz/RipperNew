@model Domain.Entities.EquipmentType

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>EquipmentType</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.EquipmentTypeName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EquipmentTypeName)
            @Html.ValidationMessageFor(model => model.EquipmentTypeName)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
