@model Domain.Entities.EquipmentType

@{
    ViewBag.Title = "Create";
}


@using (Html.BeginHorizontalForm())
{
    @Html.AntiForgeryToken()


    <fieldset>
        <legend>Dodaj nową kategorie sprzętu</legend>

        <div class="control-group">
            @Html.LabelFor(model => model.EquipmentTypeName, new { @class = "control-label" })
            <div class="controls">
            @Html.EditorFor(model => model.EquipmentTypeName)
            @Html.ValidationMessageFor(model => model.EquipmentTypeName)
        </div></div>

   <div class="control-group">
                   <div class="controls">
            <input type="submit" value="Dodaj" class="btn btn-primary" />
       </div></div>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
